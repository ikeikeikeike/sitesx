defmodule Sitesx.Support.HomeController do
  use Phoenix.Controller
  def index(conn, _params), do: conn
end

defmodule Sitesx.Support.EntryController do
  use Phoenix.Controller
  def release(conn, _params), do: conn
end
